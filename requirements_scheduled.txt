gradio==6.12.0
# 说明：为保证可复现，建议锁定 gradio 版本（当前默认 6.12.0）。
# Windows + Python 3.9：先装带 wheel 的 greenlet，避免缺少 MSVC 时从源码编译失败
greenlet>=3.0.1,<3.2
beautifulsoup4
dashscope
markdown
requests
python-dotenv
weasyprint>=52.5
latex2mathml>=3.75.0
mcp>=1.0.0
