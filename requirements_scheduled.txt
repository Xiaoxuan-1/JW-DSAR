gradio
# Gradio 4.x 依赖 huggingface_hub 的 HfFolder；1.0+ 已移除，需锁定 0.x
huggingface_hub>=0.19.3,<1.0
# Windows + Python 3.9：先装带 wheel 的 greenlet，避免缺少 MSVC 时从源码编译失败
greenlet>=3.0.1,<3.2
beautifulsoup4
dashscope
markdown
requests
python-dotenv
weasyprint>=52.5
latex2mathml>=3.75.0
